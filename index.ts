/**
 * Context: fields...
 *
 * Zod schema -> text, number, email etc.
 *
 * renderForm(schema, options) -> controls
 *   controls.name
 *   controls.email
 *   ...
 */
export const example = () => {
  console.log("Hello world");
};
